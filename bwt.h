#include <vector>

std::vector<unsigned char> shift_left(const std::vector<unsigned char>& mas);

std::vector<unsigned char> bwt_encod(const std::vector<unsigned char>& mas);

std::vector<unsigned char> bwt_decod(const std::vector<unsigned char>& mas);
